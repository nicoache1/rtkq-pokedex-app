import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    height: 80,
    flexDirection: 'row',
    marginVertical: 8,
  },
  infoContainer: {
    flex: 2,
    justifyContent: 'center',
  },
  flexible: {
    flex: 1,
    justifyContent: 'center',
  },
  imageContainer: {
    flex: 1.5,
    alignItems: 'center',
    justifyContent: 'center',
  },
  image: {
    height: 80,
    width: 140,
    borderRadius: 16,
  },
})
